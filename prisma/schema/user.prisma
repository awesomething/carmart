model User {
    id          String      @id @default(cuid())
    email       String      @unique
    hashed_pass String
    sessions    Session[]
    created_at   DateTime    @default(now())
    updated_at   DateTime    @updatedAt

    @@map("users")
}